# Google Colab Optimized Requirements
# Note: Many packages are pre-installed in Colab (numpy, pandas, scipy, matplotlib, etc.)
# Only install what's necessary to avoid conflicts

# Core data processing (usually pre-installed in Colab, but ensure compatibility)
anndata>=0.9.0
scanpy>=1.9.0

# PyTorch - Colab comes with PyTorch pre-installed, but ensure compatibility
# Uncomment if you need to upgrade:
# torch>=2.0.0
# torchvision>=0.15.0

# Training utilities
accelerate>=0.24.0
transformers>=4.34.0

# Evaluation & metrics (scikit-learn usually pre-installed)
scikit-learn>=1.3.0
seaborn>=0.12.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
click>=8.1.0
